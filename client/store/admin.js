import axios from 'axios';

/** Action Types
 *
 */

/**
 * Action Creators
 */

/**
 * Thunk Creators
 */

export default function cartReducer(state = [], action) {
  switch (action.type) {
    default:
      return state;
  }
}
